package web

const (
	apiVersion = "v1"

	// ProjectsURL is the URL path to manage Projects
	ProjectsURL = "/" + apiVersion + "/projects"

	// PullRequestsURL is the URL path to manage Pull Requests
	PullRequestsURL = "/" + apiVersion + "/pull_requests"

	// UsersURL is the URL path to manage Users
	UsersURL = "/" + apiVersion + "/users"
)
